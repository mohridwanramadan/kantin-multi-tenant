# Kantin Multi-Tenant

Sistem aplikasi kantin multi-tenant berbasis Laravel.

## Requirements

- PHP ^8.2
- Composer
- Node.js & NPM
- Database (MySQL / PostgreSQL / SQLite)

## Setup & Installation

1. Clone repository
2. Install dependencies:
    ```bash
    composer install
    npm install
    ```
